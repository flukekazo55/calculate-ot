{
  "buildCommand": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
  "outputDirectory": "frontend/dist/frontend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
